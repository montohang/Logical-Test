function dictionary(firstWord, secondWord) {
	let data = [ 'hot', 'dot', 'dog', 'lot', 'log' ];

	let isDataFound = false;
	let words = [];

	for (let i = 0; i < data.length; i++) {}
}

dictionary('hot', 'dog');
